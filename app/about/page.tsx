import React from 'react';
import db from '@/utils/db';
export default async function About() {
  /*
In this about page  this is how the structure is going to  be:
- There will be a 3 grid gallery of 6 images just like that in the dride theme.
- The images will be of different bikes scenarios
- You will import the Section Title and customize it to say "Our Gallery"
- You will import the Image component and use it to display the image inside the 3 grid components.
  */
  return <div className="min-h-screen flex-1">About page</div>;
}
